# Archived: legacy Vite admin panel prototype

This was an early, unfinished Vite + React prototype for a third "admin
panel" client, previously checked out at the repo root. It was never wired
to the backend (no API calls anywhere in it) and its pages are placeholders.

It is **not** part of the active system. The real clients are:

- `frontend/` — Next.js web app
- `mobile_app/` — Flutter app

Kept here for reference only; safe to delete entirely if no one needs it.
